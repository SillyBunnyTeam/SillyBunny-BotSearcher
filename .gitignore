# No trailing slash: during development this is a symlink to SillyBunny's tree,
# and a directory-only pattern would not match it.
node_modules
*.log
.DS_Store
bun.lock
# Token-signing secret, generated on first run. Per install, never committed.
.cursor-key
# Host-managed exact-release pin metadata.
.sillybunny-release.json
